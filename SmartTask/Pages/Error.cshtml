@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Fejl.</h1>
<h2 class="text-danger">Der skete en fejl.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Anmodnings ID:</strong> <code>@Model.RequestId</code>
    </p>
}